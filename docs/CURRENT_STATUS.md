# Current Status

> Weekly-refreshed snapshot of what's in flight and where dev ↔ prod stand.
> History lives in `docs/CHANGELOG.md`. Specs in `docs/superpowers/specs/`.
> Last updated: **May 3, 2026 (morning hotfixes)** — caught two bugs after digests went silent at 7 AM IST: (a) yesterday's cron-fix script renamed scheduleTrigger nodes but didn't update the workflow's `connections` map → trigger fired in isolation; (b) workflows imported via REST API don't inherit `GENERIC_TIMEZONE` env, fall back to UTC for cron evaluation → `0 0 7 * * *` was firing at 12:30 IST. New `scripts/fix-n8n-orphaned-connections.ts` (self-healing rewire after rename) + `scripts/fix-n8n-workflow-timezone.ts` (sets `settings.timezone: "Asia/Kolkata"` on all 32 workflows). Re-pushed; tomorrow morning's digests should land at correct IST times. n8n state confirmed: 32 of 33 active. **Meta `erp_alert` template approved + applied** late May 2 — all 47 WhatsApp Send nodes flipped to template mode (`erp_alert|en`) so messages reach any recipient regardless of 24-hour service window. **Backup cron LIVE** — host-side script (replaces broken n8n executeCommand workflow) uploads daily 02:00 IST tar.gz to Supabase Storage `n8n-backups/` on DEV bucket (PROD project paused; will switch when team validates DEV ~1 month). **Last updated** prior: May 2, 2026 (late evening + post-deploy hotfix) — added a 5th thread: hotfix for `/cash` crash + extended triage access. **(5) Hotfix:** `/cash` was throwing because `OrphanBanner` called `getOrphanCounts()` wrapped in `unstable_cache` — that cannot use `cookies()`/`headers()` and `createClient()` reads cookies internally, so the banner crashed every render and took the page down. Dropped the wrapper. Same fix also restored the missing `getOrphansByStatus` function in `orphan-triage-queries.ts` (commit `8072bde` had imported it without landing the definition — `pnpm check-types` had been broken since). Added `project_manager` to the triage allow-list (page guard + action guard + audit-table RLS via **migration 102**) so Manivel (project_manager) can attribute alongside Prem (already covered as marketing_manager) and Vivek — both will run the BOM-checking work. Mig 102 applied to dev. → migration 102. **(4) HubSpot incremental import (May 2 export):** New `scripts/import-hubspot-incremental.ts` brought 89 missing-and-non-junk deals into ERP — 82 new leads + 7 link-only + 46 proposals + 46 projects (24 of those flagged `[Likely-Duplicate-Reconcile]` for legacy PV refs from /23, /24, /24-25). 84 `To check` deals skipped as junk per Vivek's call. New `scripts/check-hubspot-payments-vs-erp.ts` cross-checked the Payments-pipeline CSV (53 deals) against ERP — found only 14/53 match by name and 39 don't have a matching project (incl. ₹15.6L Jains-Advaya, ₹11L Jains-Aadheeswar, ₹8.6L Radiance Splendour, ₹8.5L Prestige Ooty); 53 ERP projects with ₹92.4L outstanding mostly Zoho-tracked, not in HubSpot. Three independent threads from earlier today below. **(1) n8n Phase 4 + Gmail OAuth:** 5 silently-broken Tier 1 workflows fixed (`12, 15, 16, 17, 18` shipped Phase 1 with placeholder SIMULATED SEND nodes that never got converted), role routing per Vivek (Liaison=Marketing/Sales head, Finance/OM/HR=Vivek with Vinodh parallel cc until heads hired), `#56` droplet-health redesigned to a daily heartbeat (n8n removed `executeCommand` for security in current version), Gmail OAuth completed via SSH-tunnel workaround, `#55 Global Error Handler` activated. **n8n state: 20 active workflows.** **(2) Proposal financial corruption recovery — Phases 1+2+3 complete:** `scripts/reimport-hubspot-financials.ts` reset **35 corrupted HubSpot proposals** + left **109 alone** (already plausible). Vivek triaged the 5 Tier B caveated → migration **092** recovered 3 (PV128/25-26 + PV134/25-26 also got `system_size_kwp` updates; SHIROI/PRP/2024-25/0030 had a doc-internal typo). Migration **093** dropped the HubSpot carve-out from `proposal_total_sanity`. Migration **095** recovered PV333/24 after pdf-parse v2 retry. **Final state on dev:** 752 proposals · 165 flagged `financials_invalidated` · 34 soft-banner-flagged · 199 banner-visible · 0 violating the Rs.10L/kWp constraint. **(3) Zoho Orphan Triage UI shipped on `claude/kind-keller-9280a0`:** `/cash/orphan-invoices` three-pane UI for founder + finance + marketing_manager to clear 303 unattributed invoices (~₹63 Cr) + 659 payments. **Six new migrations 096–101** (renumbered from 088–093 to avoid filename collision with main's 088–095). 923 line items backfilled from Invoice.xls. Append-only audit log on-page; banner on `/cash` (cached 60s, auto-hides at 0). Spec `2026-05-01-zoho-orphan-triage-design.md`, plan `2026-05-01-zoho-orphan-triage.md`. Earlier May 2: Tasks "Client" column + leads.map_link + Expected Orders/Payments dashboard cards (mig 094); proposal financial corruption Phases 1+2 merged (migs 089/090/091, 090b). May 1: `feat/n8n-workflow-scaffolding` merged into `main` (29 commits, n8n WhatsApp Phase 3 deployment, 19 of 20 workflows live end-to-end tested). Vercel auto-deploys to `erp.shiroienergy.com`. Bundle (week to date): lead status RLS fix + `/payments/tracker` tab (mig 088); Zoho data-accuracy pass (migs 084/086/087 + phases 06–12 rewrite); full n8n Tier 1 + Tier 2 + Tier 6 scaffolding (32 workflow JSONs, event-bus router, error handler, push script); WhatsApp Business Cloud API LIVE on droplet; dashboard pipeline value scoped to negotiation-only; LLP legal-name corrections. **DigitalOcean port-block ongoing**: ports 80/443/8080 still blocked despite abuse-ticket responses; new regular Support ticket filed; SSH tunnel (`ssh -N -L 5679:172.18.0.2:5678 root@68.183.91.111`) is the management path until ports are restored. **Meta `erp_alert` template**: pending review (free-form text messages limited to Meta's 24h service window until approved — once approved, all 22+ WhatsApp Send nodes flip to template mode).

---

## Phase

**Phase 3 — Advanced Features + Deployment.**
Building out final modules before moving to full prod rollout. Still active development — 3 more big modules to ship before employee testing week.

---

## In flight this week (April 14 – May 2, 2026)

| Item | Owner | Status | Detail |
|------|-------|--------|--------|
| **Project Data Review Triage — `/data-review/projects`** | Claude (2026-05-02) | ✅ Shipped May 2 (dev) — ready for Vivek/Prem/Manivel to triage | Three-role UI (founder + marketing_manager + project_manager) to confirm or mark-duplicate the 46 HubSpot-imported projects flagged `financials_invalidated` / `system_size_uncertain`. KPI strip (Needs Review / All / Confirmed / Duplicates), expandable row edit (kWp + ₹ inputs + Save&Confirm / Confirm-no-change / Mark-Duplicate), duplicate-search modal with data-richness score comparison, audit log tab with Undo. Dashboard banner on founder + PM dashboards. Layout guard for unauthorized roles. 3 Playwright smoke tests. Uses migration 102 (already applied dev — `project_review_audit` table + `review_status` column + 3 RPCs). **To unblock:** brief Vivek/Prem/Manivel to start triaging at `/data-review/projects`. |
| **HubSpot incremental import (May 2 export) + Payments-pipeline cross-check** | Claude (2026-05-02 evening) | ✅ Imported on dev — 39 Payments deals need triage | Vivek delivered fresh `hubspot-deals-2026-05-02.csv` (1,202 unique deals) + `hubspot-deals-payments-2026-05-02.csv` (53 deals in Payments pipeline). Diff vs DB → 173 deals in CSV but missing from ERP, of which 84 `To check` (junk per Vivek) skipped, 89 imported (53 Closed Won + 14 lost + 22 in-flight). New `scripts/import-hubspot-incremental.ts` mirrors `migrate-hubspot.ts` patterns but with strict exact-name matching + an ambiguity check (the first attempt wrongly merged different "Mr Saravanan"s, "Mr. Kumar" → "Mr Jeyakumar", "Mr. Raja" → "Mr Babu Govindarajan"). **Result:** 82 leads inserted + 7 leads linked (hubspot_deal_id back-filled), 46 proposals + 46 projects inserted (all with `financials_invalidated=TRUE` since CSV TPV missing/implausible across the board, and 41 with `system_size_uncertain=TRUE`). 24 projects with legacy PV refs (`/23`, `/24`, `/24-25`) flipped to `status=completed` and tagged `[Likely-Duplicate-Reconcile]` in notes. Final state: leads 1029→1111 hubspot-linked, proposals 144→190, projects 316→362. **Payments cross-check finding:** Sales/Payments pipelines use different Record IDs, so name-match required. Only **14/53 Payments-CSV deals match an ERP project by exact name**; **39 don't have a matching project at all** — these include big-ticket items: Jains-Advaya ₹15.6L, Jains-Aadheeswar ₹11L, Radiance Splendour ₹8.6L, Prestige Ooty ₹8.5L, Jains-Adidev ₹6.8L, Jains-Seven77 ₹6.8L, DRA-Downtown ₹4.66L, S&P Courtyard ₹4L, Lancor-Ananda ₹2.39L, Lancor-Bakya ₹2.39L. Conversely, 53 ERP projects with outstanding receivables totaling ₹92.4L are mostly Zoho-tracked (not in HubSpot Payments). Of the 14 matches, 13 fall in Bucket B (project exists in ERP but no invoice raised yet — actionable: raise invoices). **To unblock:** (1) Vivek to triage the 39 Payments-pipeline-only deals — for each: does the project actually exist in ERP under a different name, or do we need to create it? (2) Review the 24 `[Likely-Duplicate-Reconcile]`-flagged projects (`SELECT * FROM projects WHERE notes ILIKE '%[Likely-Duplicate-Reconcile]%'`) and merge with their Drive-imported counterparts where applicable. Scripts: `analyze-hubspot-csv-vs-erp.ts`, `import-hubspot-incremental.ts`, `check-hubspot-payments-vs-erp.ts`. |
| **Hotfix: `/cash` crash + extend triage access to project_manager** | Claude (2026-05-02 evening) | ✅ Shipped May 2 | Vivek post-deploy: *"cash flow in Finance is just not showing. It is giving an error… Also the BOM checking will be done by Prem and Manivel, so they should both have access to the triage that you have created."* Three things: (1) `OrphanBanner` on `/cash` was wrapping `getOrphanCounts()` in `unstable_cache` — that can't use `cookies()`/`headers()`, but `createClient()` reads cookies internally. Every render of the banner threw and crashed `/cash`. Dropped the wrapper (RPC <10ms, per-request fine) + the now-no-op `revalidateTag('orphan-counts')` from `postSuccess()`. (2) Earlier lint refactor `8072bde` had imported `getOrphansByStatus` from `orphan-triage-queries.ts` but never landed the function — `pnpm check-types` had been broken since. Added the function (canonical no-inline-Supabase pattern, NEVER-DO #15) and aligned `_client-fetchers.ts` to delegate to it. (3) Verified roles: Premkumar=`marketing_manager` (already in), Manivel Sellamuthu=`project_manager` (not in). Added `project_manager` to the page guard, action guard, and audit-table RLS via **migration 102** (drops + recreates the two policies with the wider role list). Mig 102 applied to dev. Commits `9a1d176` + merge `aedd70c`. |
| **Zoho Orphan Triage UI — `/cash/orphan-invoices`** | Claude (2026-05-01/02 subagent run) | ✅ Shipped May 2 (dev) — awaits team triage | Three-pane UI for founder + finance + marketing_manager + project_manager (added in mig 102 hotfix) to clear the 303 unattributed Zoho invoices (~₹63 Cr) and 659 payments left after mig 087's 1:1 backfill. Outcomes per row: assign / no ERP match / defer / reassign / undo. Append-only audit log on-page with decision filter. Banner on `/cash` (auto-hides at 0). **6 migrations 096-101** (renumbered from 088-093 to avoid filename collision with main's 088-095) — line items table + backfill script (923 line items inserted, 0 mismatches after the pre-GST-vs-post-GST comparison bug was caught), `excluded_from_cash` + `attribution_status` columns + indexes + seed (303 invoices/659 payments enter the queue), cash trigger filters `excluded_from_cash IS NOT TRUE` (counts unchanged at 316/41/116/159), `zoho_attribution_audit` table + RLS, atomic SQL helpers (`assign_orphan_invoice` / `exclude_orphan_invoice` / `reassign_orphan_invoice` with cascade rules per spec), three read RPCs. Two reconciliations during apply: actual_start_date/actual_end_date column names + LOWER-inside-REGEXP_REPLACE bug. **To unblock:** brief Prem + Manivel + finance to start triaging via Slack. Spec `2026-05-01-zoho-orphan-triage-design.md`, plan `2026-05-01-zoho-orphan-triage.md`. |
| **n8n Phase 4 — 5 silent-no-op workflow fixes + role routing + Gmail OAuth + #55 active** | Claude + Vivek (2026-05-02) | ✅ Shipped May 2 | Five Tier 1 workflows (`12-ceig-approval-received, 15-om-ticket-created, 16-expense-submitted, 17-leave-request-submitted, 18-employee-created`) shipped Phase 1 with placeholder SIMULATED SEND `Set` nodes that never got converted to real WhatsApp Send nodes — they were active since 2026-05-01 but silently no-oped on every event. New `scripts/fix-simulated-send-nodes.ts` walks the 5 files and replaces SIMULATED SEND with canonical `n8n-nodes-base.whatsApp` (typeVersion 1.1, `whatsAppApi` credential ref); `#18` swaps in two parallel sends for HR + IT. Per Vivek's directive: **Liaison = Sales head's phone (same person)**, **Finance/O&M/HR = Vivek + Vinodh until heads are hired** — set `LIAISON_HEAD_WHATSAPP=+919444060787`, `FINANCE_HEAD_WHATSAPP=OM_HEAD_WHATSAPP=HR_HEAD_WHATSAPP=+919444414087` on droplet, and extended `scripts/add-vinodh-cofounder-fanout.ts` to add a parallel "Send WhatsApp to Vinodh" node alongside primary send in `24, 25, 27` digests; `08-vendor-payment-due` gets "Send to Vinodh (always)" parallel to "Send to Finance (always)". `#56 Droplet health` rebuilt as a daily heartbeat (n8n 1.x removed `executeCommand` for security; for metric thresholds use DO's built-in monitoring). **Gmail OAuth completed** via SSH-tunnel workaround: temporarily switched `WEBHOOK_URL`/`N8N_EDITOR_BASE_URL` from `https://n8n.shiroienergy.com/` to `http://localhost:5679/` so Google's post-sign-in redirect went through the tunnel instead of the DO-blocked public URL; OAuth credential `Gmail (Vivek)` (scope `gmail.send`) now stored encrypted in n8n's DB; reverted env vars to production. **`#55 Global Error Handler` activated** (workflow ID `8v9P5gxXqPfYNWOp`) — every workflow's `errorWorkflow` setting already pointed at it, so any node failure in the 19 currently-active production workflows now auto-emails `svivek.88@gmail.com`. **n8n state: 20 active workflows** (router + 16 Tier 1 sub-workflows + Vivek daily 7AM digest + Global Error Handler + legacy "My workflow"). Detailed Gmail-OAuth-via-tunnel runbook + DO-port-block status section added to `infrastructure/n8n/workflows/README.md` for future reruns. **Pending**: Meta `erp_alert` template approval (so digests can fire to non-tunnel recipients), DO port restoration (so ERP can call the router from public internet), Sentry → `#58 Sentry forwarder` webhook config, Supabase Storage bucket `n8n-backups` for `#57`. |
| **Tasks Client column + leads.map_link + Expected Orders/Payments cards** | Claude (overnight 2026-05-01 → 2026-05-02) | ✅ Shipped May 2 | Four asks bundled into migration 094 (originally numbered 089, renumbered after origin/main shipped its own 088-091 in parallel) + matching UI: (a) backfill query for `payment_followup`/`payment_escalation` tasks (no-op at apply time — 0 open of those categories — kept as future-safety); (b) `leads.map_link TEXT NULL` mirroring `projects.location_map_link`, with optional input on `/sales/new`, "View on map ↗" row on `/leads/[id]`/`/sales/[id]` Contact Info, and a maps link in the design workspace header; trigger `create_project_from_accepted_proposal` updated so projects auto-inherit `lead.map_link`; (c) `/tasks` "Project Name" → "Client" rename with second-pass enrichment that resolves customer name for lead-linked tasks (entity_type='lead', no project_id) — they previously showed "—"; (d) RPCs `get_expected_orders(window_days)` + `get_expected_payments(window_days)` powering new "Expected Orders" + "Expected Payments" cards on `/dashboard` (founder + marketing_manager fall-through), and the Expected Payments card on `/cash` between KPIs and project positions. Two-section "This Week" / "Later This Month" layout per card. All aggregation in SQL (NEVER-DO #12). Spec: `docs/superpowers/specs/2026-05-01-task-sync-map-link-expected-orders-design.md`. |
| **Proposal financial corruption recovery — Phases 1+2+3 complete** | Claude (worktree `friendly-montalcini-e601d1` → merged May 2) | ✅ Shipped May 2 | **Phase 1:** importer guardrails in `excel-parser.ts` (per-kWp ceiling + capex-summary row rejection) + `extract-bom-all-unprocessed.ts` (filename-must-match-customer) + `migrate-hubspot.ts` (TPV sanity check) + Vitest coverage. pdf-parse v2 fix later applied to `create-proposals-from-docs.ts` + `ai-extract-pipeline.ts`. **Phase 2 Part 1:** migrations 089/090/091 — schema flags + Tier A reset of 130 corrupted proposals + CHECK constraint at ₹10L/kWp. **Phase 2 Part 2:** AI re-extraction across 17 Tier B → 7 clean recoveries via mig 090b. UI yellow banner on `/proposals/[id]` and `/leads/[id]/proposal` surfaces `financials_invalidated`, `system_size_uncertain`, and per-kWp > ₹2L soft trigger. **Phase 3:** Vivek delivered 2026-05-02 HubSpot CSV → `scripts/reimport-hubspot-financials.ts` reset **35** corrupted HubSpot proposals + left **109** alone (already plausible) + 0 CSV-driven updates (export had no TPV for historical Closed Won deals). Vivek's call on 5 caveated Tier B → mig 092 recovered 3 (PV128/PV134 also got kWp updates, SHIROI/PRP/2024-25/0030 had a doc typo); PV281 + PV279 confirmed as different projects (banner only). Mig 093 dropped HubSpot carve-out from `proposal_total_sanity`. Mig 095 recovered PV333/24 (₹3,00,346 / 3.78 kWp). **Result:** 199 banner-visible proposals, 0 DB-constraint violators. Plan: `2026-04-30-proposal-corruption-implementation.md`. Design: `2026-04-26-proposal-financial-corruption-recovery.md`. |
| **n8n Phase 3 deployment — WhatsApp pipeline LIVE** | Claude + Vivek (2026-05-01) | ✅ Deployed May 1 | Closed out the n8n branch end-to-end: pushed all 20 workflows to `n8n.shiroienergy.com`, **19 active and tested**. Discovered Phase 1 left two latent bugs in WhatsApp Send nodes (wrong credential type key `whatsAppBusinessAccountApi` instead of canonical `whatsAppApi`; wrong parameter names `toPhoneNumber`/`message` instead of `recipientPhoneNumber`/`textBody`); new `scripts/fix-n8n-whatsapp-nodes.ts` rewrote all 22 send nodes to canonical schema (typeVersion 1.1). DigitalOcean blocked external ports 80/443/8080 in response to Netcraft alert despite our abuse response — droplet itself is healthy, all management routed via SSH tunnel `ssh -L 5679:172.18.0.2:5678 root@68.183.91.111`. Created 3 n8n credentials via REST API: `WhatsApp (Shiroi)` (whatsAppApi) · `x-webhook-secret` (httpHeaderAuth) · `Supabase service role` (httpHeaderAuth `apikey:sb_secret_*`). Updated droplet `.env` with `env_file:` directive in docker-compose; populated `VIVEK_WHATSAPP=+919444414087`, `VINODH_WHATSAPP=+919444052787`, `SALES_HEAD_WHATSAPP=+919444060787`, `DESIGN_HEAD_WHATSAPP=+919704514879`, `PROJECTS_HEAD_WHATSAPP=+919486801859`, `PURCHASE_HEAD_WHATSAPP=+919698685985`, `SUPABASE_PROJECT_ID=kfkydkwycgijvexqiysc`, `SUPABASE_SECRET_KEY=<sb_secret prod>`. Vinodh added as co-founder cc on all founder-tier workflows via new `scripts/add-vinodh-cofounder-fanout.ts` — patches `01`, `19`, `28`, `56`, `58` (always cc) and `08` (cc only if amount >₹5L). End-to-end test 2026-05-01: webhook fires → WhatsApp lands on +919444414087 in <2 sec via Meta Cloud API. **Three workflows still inactive on purpose**: `55` (Gmail OAuth needed), `56` (n8n disables `executeCommand` by default — needs redesign), `57` (Supabase Storage bucket `n8n-backups` not created), `58` (Sentry not yet pointed at webhook). **Track B (Meta WhatsApp templates)** in flight: free-form text only works inside Meta's 24h service window; Vivek submitted `erp_alert` utility template with 2 vars + literal header/footer ("Hi from Shiroi Energy" / "Thank you. Shiroi Energy ERP") — pending Meta review. Once approved, all 22 send nodes flip from `text` → `template` mode in a follow-up commit. **Pending**: DigitalOcean ticket escalation (workflows live but unreachable from public internet until ports restored). |
| **Branch merge: `feat/n8n-workflow-scaffolding` → `main`** | Vivek + Claude (2026-05-01) | ✅ Merged + Deployed May 1 | 29-commit merge brings everything below into `main`. Vercel auto-deployed to `erp.shiroienergy.com`. Prem + team see the changes immediately. Branch deleted post-merge. |
| **Fix: lead status RLS for sales_engineer + Payments Tracker tab** | Claude (2026-05-01) | ✅ Shipped May 1 | Prem's lead-status updates were silently failing. Root cause: `profiles.role = 'sales_engineer'` but `leads_update` RLS excluded that role. Three-layer fix: (a) one-shot data correction `role → marketing_manager`; (b) migration 088 adds `sales_engineer` to `leads_update`; (c) `status-change.tsx` + `inline-edit-actions.ts` + `bulkChangeLeadStatus` now use `.select('id')` + 0-row detection. Same migration ships RPC `get_payment_tracker_rows()` for new `/payments/tracker` tab: per-project rollup with KPI strip + 6 filter badges. Aggregations entirely in SQL. Live numbers: 299 projects, 238 with outstanding, ₹13.75 Cr open, 159 over 60 days. |
| **Fix: dashboard pipeline value too big — `feat/n8n-workflow-scaffolding`** | Claude (overnight 2026-04-26) | ✅ Shipped Apr 26 | Vivek: *"Pipeline value on the dashboard is too big. I want to see pipeline value of only what is in negotiation not the entire pipeline."* `getSalesDashboardData` was summing `proposals.total_after_discount` where status IN (draft, sent, viewed, negotiating) — swept the entire active funnel AND was inflated by data corruption (38 proposals show >₹10 Cr each, max ₹985 Cr for a 4.4 kWp residential — separate task). Switched to existing `get_lead_stage_counts` RPC and pulled the 'negotiation' bucket only (`SUM(estimated_size_kwp × 60000)`). KPI relabelled "Negotiation Pipeline" with subnote "N leads in negotiation". Dev: ₹1,372 Cr → ₹1.24 Cr from 15 leads. Commit `bd82559`. |
| **Fix: net position negative on a lot of projects — `feat/n8n-workflow-scaffolding`** | Claude (overnight 2026-04-26) | ✅ Shipped Apr 26 | Vivek: *"The net position is negative in a lot of projects. This is not right. Pls check this in detail."* Root cause: 324 of 481 zoho_import invoices (₹64.3 Cr) and 715 of 1078 zoho_import customer_payments (₹59.1 Cr) had NULL project_id — Phase 08 attributed only via Zoho Project ID/Name, not Customer Name; Phase 09 inherited from invoice. So when an invoice lacked a project tag (common for parent-customer-level billing), the entire customer cash trail orphaned and projects appeared "negative". **Migration 087** adds `zoho_customer_id` + `zoho_customer_name` columns to invoices/customer_payments (indexed). Phase 08/09 import scripts updated to populate these going forward. **`scripts/backfill-zoho-customer-attribution.ts`** does conservative 1:1 attribution via exact + token-subset matches against ERP customer names, drops generic ERP names (subset of others), drops fuzzy fallback (over-matched parent-company names). Result on dev: 21 invoices + 50 cascaded payments + 6 advances attributed (₹1.51 Cr / 16 projects); 481 invoices + 1000 payments now have zoho_customer_name+id captured. Sheela Green flipped −₹15K → +₹1.3L. **Side-effect**: bulk refresh exposed previously-stale projects, negative count went 16 → 41 — these are real cash holes (vendor work done, no Zoho invoice exists), not new bugs. Remaining 303 invoices / ~₹63 Cr are parent-company invoices ("LANCOR HOLDINGS" → 12+ Lancor projects) that are fundamentally ambiguous without a manual mapping UI. Commits `daeda23`, mig `087`. |
| **Zoho data-accuracy pass** | Claude (overnight 2026-04-19/20) | ✅ Merged May 1 | Vivek flagged financials as still wrong after migs 079/080/081 and authorized an overnight autonomous pass: *"Find the best route to get all this data right and go ahead and implement them overnight."* Rewrote phases 06–12 of `scripts/zoho-import/` end-to-end. Phase 06 matcher is now a weighted scorer (Jaccard + Dice + project-cost/size/city/customer) — auto-matches jumped 12 → 141. Phases 07/08 read `Project ID` directly from Zoho XLS (lookup via `projects.zoho_project_id`). Phases 09/11 fixed the grain bug — Customer_Payment.xls and Vendor_Payment.xls are **per-allocation** (one XLS row per invoice/bill a payment is split across); prior code deduped by parent `CustomerPayment ID` / `VendorPayment ID` and dropped 99% of detail. New dedupe keys: `InvoicePayment ID` / `PIPayment ID`. Phase 11 also ditched the "first open bill for vendor" heuristic (which misdirected 669 of 729 in April's import) for a direct `Bill ID` → `zoho_bill_id` lookup. Phases 10/12 added customer-name fallback. **Migration 084** relaxes `project_id` to NULL on `purchase_orders` / `invoices` / `customer_payments` / `vendor_payments` so rows without a matched project still land (CHECK constraint keeps ERP-source rows NOT NULL). **Result:** customer_payments 7 → 1078 (154×), vendor_payments 729 → 1486, invoices 20 → 481, POs 106 → 1247, expenses 190 → 5113. Reconciliation: **2 discrepancies across 141 mapped projects** (one project off ₹18,775). Money totals now captured — ₹67.87 Cr received, ₹46.72 Cr paid, ₹74.74 Cr invoiced, ₹64.52 Cr billed-to-us, ₹6.53 Cr expensed. Plan: `docs/superpowers/plans/2026-04-19-data-accuracy-pass.md`. 76 projects still in `docs/zoho-review-queue.csv` for manual match. **To unblock morning:** (1) review money totals against bank/accountant statements; (2) spot-check a sample of projects in `/profitability`; (3) manually match remaining 76 projects (sheet already present); (4) merge branch. |
| **n8n workflow scaffolding (Tier 1 + Tier 2 + Tier 6)** | Claude (overnight 2026-04-18/19/20) | ✅ Merged May 1 | Branch has 10+ commits ready for Vivek to review + merge. Tier 1 of the 59-workflow catalog is **complete**. **Prior commits still pending merge:** LLP legal-name corrections · `emitErpEvent()` helper + `N8N_EVENT_BUS_URL` env · migration 082 `employees.whatsapp_number` · event-bus router + global error handler + programmatic push script · 4 Tier 1 sub-workflow templates · emit wired into `advanceProjectStatus` + `submitExpense` · migration 083 (7 digest views) + WhatsApp template catalog (~20 templates). **New this pass (overnight 2026-04-19/20):** (a) `emitErpEvent` wired into 5 more action files — `approvePO` → `purchase_order.approved`, `recordPayment` → `customer_payment.received`, `createServiceTicket` → `om_ticket.created`, `createEmployeeAccount` → `employee.created`, `recordCeigApproval` → `ceig_approval.received`. Pre-existing embed bug in po-actions.ts (`vendors(name)` → `(company_name)`) fixed along the way. (b) 11 more Tier 1 webhook sub-workflow JSONs (04 proposal-requested, 05 proposal-submitted, 07 PO-approved, 09 GRN-recorded (pending ERP wiring), 10 installation-scheduled, 11 installation-complete, 12 CEIG-approval-received, 14 customer-payment-received, 15 om-ticket-created, 17 leave-request-submitted (pending ERP wiring), 18 employee-created) + 2 Tier 1 cron JSONs (03 lead-stale-24h hitting v_digest_leads_stale_24h, 08 vendor-payment-due hitting v_digest_vendor_payments_due_7d with >₹5L → Vivek cc branch and MSME 45-day inline reminder). All 13 use SIMULATED SEND `Set` placeholders until WABA credential lands. (c) Router `00-event-bus-router.json` expanded from 5 to 16 Switch routes + fallback. (d) Migration 085 (applied to dev via MCP) adds 8 more digest views for Tier 2 (`v_digest_proposals_silent_3d`, `v_digest_proposals_design_backlog`, `v_digest_milestones_overdue`, `v_digest_pos_pending_approval`, `v_digest_vendor_payments_due_7d`, `v_digest_invoices_due_7d`, `v_digest_om_tickets_open_48h`, `v_digest_leave_pending`). Pre-apply fix: vendors table has `company_name` not `name` — corrected throughout. (e) `scripts/strip-view-fk-entries.mjs` introduced — post-processes `database.ts` to drop bogus `referencedRelation: "v_*"` FK entries that Supabase's type generator invents from view metadata (without it, tsc hits TS2589 "type instantiation excessively deep"). Cuts database.ts from ~36k lines → ~15k. Run after every regen. (f) Push script + workflows README updated with all new placeholders, Supabase service-role credential, and Tier 1 status matrix. Type-check + lint clean repo-wide. **Tier 2 also shipped same pass:** 10 daily/weekly digest JSONs (`19`–`28`) following the 4-node cron template (scheduleTrigger → Supabase REST → compose → SIMULATED SEND). `19` (Vivek 7AM, new leads), `20–25/27` (role-head 8AM digests hitting primary Tier 2 views — stale leads, design backlog, overdue milestones, pending POs with MSME flag, overdue invoices, open O&M tickets with SLA badge, pending leave requests). `26` (Liaison) and `28` (Vivek weekly Mon) are placeholder workflows documenting the missing views (CEIG/net-metering/docs-expiry for 26, weekly rollups for 28) — author in a follow-up migration. Adds 8 more role-head WhatsApp env vars to the droplet checklist (`{SALES,DESIGN,PROJECTS,PURCHASE,OM,LIAISON,HR}_HEAD_WHATSAPP`). **Tier 6 meta also shipped:** `56` droplet health (every 15min executeCommand top/free/df → Switch on ≥85% → alert Vivek), `57` nightly 2AM IST backup of `~/.n8n` → `tar.gz` → Supabase Storage `n8n-backups/YYYY-MM-DD.tar.gz` with sha256 (needs bucket pre-create), `58` Sentry webhook `/webhook/sentry-alert` filtering fatal/error → WhatsApp Vivek. **Templates:** 10 more WhatsApp template bodies added to `infrastructure/n8n/templates.md` for the new Tier 1 + Tier 6 surfaces (proposal-requested, proposal-submitted, po-approved, vendor-payment-due, install-complete-internal, ceig-approval, customer-payment unified, om-ticket-assigned, employee-onboarded unified, infra-alert unified). Unified templates keep Meta's finite template quota lean. No new push-script mappings needed — Tier 2/6 only reference credentials/error-handler already registered. **To unblock morning: (1)** merge branch; **(2)** finish WABA creation + submit all templates in `templates.md` to Meta for approval; **(3)** create n8n credentials (`x-webhook-secret` Header Auth, `Gmail (Vivek)` OAuth, **new: `Supabase service role` HTTP Header Auth** with `apikey: {sb_secret_*}`); **(4)** set droplet env vars (`SUPABASE_PROJECT_ID`, `SUPABASE_SECRET_KEY`, `VIVEK_WHATSAPP`, `FINANCE_HEAD_WHATSAPP`, `SALES_HEAD_WHATSAPP`, `DESIGN_HEAD_WHATSAPP`, `PROJECTS_HEAD_WHATSAPP`, `PURCHASE_HEAD_WHATSAPP`, `OM_HEAD_WHATSAPP`, `LIAISON_HEAD_WHATSAPP`, `HR_HEAD_WHATSAPP`); **(5)** create private Supabase bucket `n8n-backups` for workflow #57; **(6)** in Sentry, configure an alert-rule webhook to `https://n8n.shiroienergy.com/webhook/sentry-alert` with `x-webhook-secret` header; **(7)** run `pnpm tsx scripts/push-n8n-workflows.ts` to import all 32 workflows (router + error handler + 15 Tier 1 subs + 2 Tier 1 crons + 10 Tier 2 digests + 3 Tier 6 meta). Migration state on dev: 081 tracked + 082+083+085 tracked + 084 DDL live (applied outside `supabase_migrations.schema_migrations`). |
| **n8n + Caddy infrastructure** | Vivek + Claude | ✅ Shipped Apr 19 | DigitalOcean Bangalore droplet (`shiroi-erp`, 2 GB / $12/mo) running n8n + Caddy via `docker-compose`. Public URL `https://n8n.shiroienergy.com` (auto-HTTPS via Let's Encrypt). Cloud-init user-data script bakes Docker + UFW + `/opt/shiroi-automation/` at first boot — after two aborted attempts (stuck `sshd_config` apt prompt, then failed rebuild), the cloud-init path worked first-try. Replaces the "spare laptop" plan from master reference §3.2 — same software, different host; trades ~₹1,000/mo for public IP (so Supabase webhooks land without LAN tunneling) + zero uptime management. Infra files mirrored in `infrastructure/n8n/` (docker-compose + Caddyfile + cloud-init + README). Unblocks Phase D (Zoho live sync), daily training microlearning, WhatsApp drip sequences, nightly crons, and the bug-report webhook from `/settings`. |
| **Fix: project cash position wrong on 115 projects (root cause, not symptom)** | Claude | ✅ Shipped Apr 19 | Vivek pushed back on mig 079: *"the numbers are still wrong. VAF still shows -3 cr, whereas it should be profitable. same with a lot of other projects. you have not found the root cause for the cash position numbers. pls dig deep and get this sorted out."* Two bugs stacked. **(A)** `scripts/migrate-google-drive.ts` had pre-created 869 ERP-source POs from BOM with fake `amount_paid=total_amount` + `status='fully_delivered'`. **(B)** `refresh_project_cash_position()` trigger computed `total_invoiced` via a backwards LEFT JOIN starting from customer_payments, so projects with 0 payments showed `invoiced=0` even with real invoices. Combined effect on VAF: cash said `net=-3Cr`, truth is `+17L`. **Mig 080** deletes 75 Drive-BOM POs on 8 dup-PO projects + rewrites the trigger to query invoices/payments independently + force-refreshes all 57 cash-position rows. **Mig 081** soft-cancels the remaining 775 Drive-BOM POs on 115 non-dup projects (57 real Zoho payments worth ₹98.49L kept attached to satisfy CHECK). Post-fix: only 5 projects remain with net<0 — all legitimate "vendor-spend-ahead-of-invoice" states (real Zoho POs). Findings in `docs/2026-04-19-cash-position-root-cause.md`. |
| **Fix: vendor payment over-counting from Zoho import** | Claude | ✅ Shipped Apr 18 | Vivek flagged financial data as wrong ("VAF is fully paid and so on"). Phase 11 Zoho import linked 669/729 vendor payments to wrong bills/POs via bad fallback heuristic (first open bill / latest PO for vendor). Trigger summed unrelated payments onto single targets; `total_ap_bills` was −₹1.39Cr and `total_ap_pos` was −₹4.29Cr (negative = impossible). Migration 079 clamps `amount_paid` to `total_amount` on 2 bills + 25 POs, hardens `update_po_amount_paid` and `recalc_vendor_bill_totals` with LEAST() guards so future imports can't break the invariant. Post-fix VAF AP outstanding ₹0 as Vivek said. AR untouched. Root-cause bug in `phase-11-vendor-payments.ts` to be fixed before next Zoho sync. Findings in `docs/2026-04-18-finance-overcounting-fix.md`. |
| **Data: historical dates backfill** | Claude | ✅ Shipped Apr 18 | Zoho import clobbered `created_at` on ~600 projects/proposals/leads with the 2026-04-02 batch timestamp. Migration 073 used Zoho invoice dates (12 projects). Migration 076 used `project_number` FY (154 projects). Migration 077 parsed year from `proposal_number` (428 proposals) + cascaded to leads. **Migration 078 (follow-up):** walked 4 `Proposals YYYY` Drive folders (1,405 children), matched 229 unambiguous proposals by normalised proposal number, replaced synthetic FY-start dates with real Drive `createdTime` @ noon IST. Dispersed the 2024-01-01 cluster 96→3 and 2023-01-01 cluster 84→28. Remaining Sep-2025 clusters are SHIROI/PROP/2025-26/NNNN internal proposals (not in Drive), sit within correct FY. Full findings in `docs/2026-04-18-date-backfill-findings.md`. |
| **Fix: Expenses invisible in project Actuals tab** | Claude | ✅ Shipped Apr 18 | Migration 066 table rename preserved old FK names (`project_site_expenses_*_fkey`); PostgREST embed hints in `listExpenses` expected `expenses_*_fkey` and failed silently — project Actuals tab showed "no expenses" for all 121 projects with 1358 existing vouchers. Renamed 3 FKs. Reported by Manivel. Migration 074. |
| **User Settings Page** | Claude | ✅ Shipped Apr 18 | `/settings` route with Account / Feedback / Users (founder-only) tabs. Password change, bug reporting (with optional n8n webhook), role + active controls. New `ProfileMenu` dropdown in topbar. Migration 073 (`bug_reports` table + founder-admin RLS). 3 Playwright smoke tests. See `docs/superpowers/plans/2026-04-18-user-settings-page.md`. |
| **Finance Module V2 + Zoho import** | Claude | ✅ Shipped Apr 18 | 5 migrations (067–072). 13-phase import script: 264 accounts, 17 taxes, 945 items, 296 vendors (272 new), 12 projects matched, 2336 vendor bills, 729 vendor payments, 190 expenses. Finance UI: /vendor-bills, /vendors/[id], /profitability V2, /cash Zoho panel, MSME aging strip, sync health dashboard card. Reconcile report: 3 discrepancies. See `docs/modules/finance.md`. |
| **Expenses Module** | Claude | ✅ Shipped Apr 17 | Standalone /expenses module; dual workflow (project-linked 3-stage + general 2-stage); per-submitter voucher numbers; category master + CRUD; Project Actuals read-only embed. Migration 066. See `docs/modules/expenses.md`. |
| **Purchase v2 feedback pass** | Claude | ✅ Shipped Apr 17 | Vivek-review feedback across all 5 tabs: Tab 1 inline Qty/Rate + BOQ PDF; Tab 2 vendor typeahead + invitation rows; Tab 3 terms footer; Tab 4 Send-to-vendor (Email/WA/Copy) + founder quick-PO auto-approve; Tab 5 generated `dispatch_stage` + PM role widening + receipt cascade. See `docs/superpowers/specs/2026-04-17-purchase-v2-feedback-design.md` + plan. Migration 065. |
| **Purchase Module v2** | Claude | ✅ Shipped Apr 17 | Full 5-stage competitive pipeline (BOQ → RFQ → Comparison → PO → Dispatch), vendor portal, founder approval, audit log, 10 phases. See `docs/modules/purchase.md` + `docs/superpowers/specs/2026-04-17-purchase-module-v2-design.md`. Migration 060. |
| Plant Monitoring module shipped | Claude | ✅ Shipped Apr 16 | `/om/plant-monitoring` page, migration 059, 11 commits. See `docs/modules/om.md`. |
| Docs restructure | Claude | ✅ Shipped Apr 17 | This refactor. See `docs/superpowers/specs/2026-04-17-docs-restructure-design.md`. |
| Marketing + Design revamp — feedback loop | Prem (marketing mgr) | 🔜 Next | Get Prem's feedback on /sales + partners + design workspace + closure band UI. Same cycle as Manivel's PM feedback. |
| Zoho manual project match | Vivek | 🔜 Action needed | 76 projects in `docs/zoho-review-queue.csv` need manual match in Supabase `zoho_project_mapping` table. After matching, re-run phases 07-13 to pick up those projects' transactions. |
| Phase D (n8n Zoho live sync) | Claude | 🔜 Unblocked | n8n now running (Apr 19). DB infrastructure (zoho_sync_queue, triggers, claim/ack RPCs) was ready since migration 072. Next: build the workflow — consume `zoho_sync_queue`, call Zoho Books API, ack on success. |
| Bug-report webhook → alert | Claude | ✅ Shipped Apr 20 | `notifyBugReport` in `settings-actions.ts` now fires through the event bus router (`emitErpEvent('bug_report.submitted', …)` — with submitter full_name fetched from profiles) whenever `N8N_EVENT_BUS_URL` is set, and falls back to the legacy standalone `N8N_BUG_REPORT_WEBHOOK_URL` only when the event bus env is unset. New `01-bug-report.json` sub-workflow follows the Tier 1 template and WhatsApps `$env.VIVEK_WHATSAPP` with a severity-iconed summary + `/settings?tab=feedback&id=…` deep link. Retires the last standalone webhook on the Tier 1 surface. |
| Employee testing week | All | 🔜 Planned | 5–6 employees review on dev for 1 week. Data flags + inline edit + verification. |
| Prod deployment window | Vivek | 🔜 After testing | Batch-promote migrations 013–072 to prod + selective data migration. |

---

## Migration state

| Env | Latest applied | Pending |
|-----|---------------|---------|
| **Dev** (`actqtzoxjilqnldnacqz`) | **094** (task backfill + leads.map_link + expected orders/payments RPCs, May 1–2 via Supabase MCP). 082–094 tracked (incl. parallel work: 088 zoho_invoice_line_items, 089 proposal_data_quality_flags, 090 reset_corrupted_proposal_financials, 090b apply_tier_b_reextractions, 091 proposal_total_sanity_constraint, 091 zoho_attribution_audit, 092 orphan_triage_functions, 093 orphan_read_rpcs, 094 task_backfill_map_link_expected_rpcs). Mig 084 (nullable project_id) DDL is live but was applied outside `supabase_migrations.schema_migrations`; Zoho re-run relied on it. | None |
| **Prod** (`kfkydkwycgijvexqiysc`) | 012 (approximate — last coordinated window) | **013 through 094** — 82 migrations waiting on the next prod window. Note: live ERP at `erp.shiroienergy.com` points at **dev** Supabase, so this prod-DB gap doesn't block users today. |

**Prod deploy strategy:** batch-promote all pending migrations after employee testing week completes. Selective data migration alongside (we've heavily backfilled dev from Google Drive, HubSpot, Zoho Books, and WhatsApp; not all of that needs to move to prod — specifically the Zoho import tables are dev-only for now).

**Overnight run 2026-04-18 results** (Claude agentic):
- Migrations 067–072 applied and types regenerated (0 check-types errors)
- 13-phase Zoho Books historical import run end-to-end. Key counts: 264 accounts, 17 taxes, 945 items, 20 contacts linked, 296 vendors (272 new created), 12 projects auto-matched (76 in review queue, 160 unmatched), 106 POs, 20 invoices, 7 customer payments, 2336 vendor bills (~2147 from prior run + 189 newly fixed), 729 vendor payments, 190 project expenses, 12 credit notes all skipped (no linked invoice).
- Reconciliation: 3 discrepancies found across 12 matched projects (>₹1 tolerance). Report at `docs/zoho-import-report-2026-04-17.md`.
- Finance UI Phase C shipped (see changelog).

---

## Environment URLs

| Thing | URL |
|-------|-----|
| Dev Supabase | `actqtzoxjilqnldnacqz.supabase.co` |
| Prod Supabase | `kfkydkwycgijvexqiysc.supabase.co` |
| ERP (points at **dev** Supabase) | `erp.shiroienergy.com` (Vercel, auto-deploys on push to `main`) |
| GitHub | `github.com/Shiroi-Energy-LLP/shiroi-erp` (private) |
| Local dev | `localhost:3000` |
| n8n | `https://n8n.shiroienergy.com` (DO Bangalore droplet `shiroi-erp`, `68.183.91.111`) |
| PVLib microservice | `https://pvlib.shiroienergy.com` (same droplet, not yet deployed) |

---

## Active CI / discipline gates

Running on every PR + push to `main` (`~1 min` total):

1. `pnpm check-types` — 5 packages, 0 errors required
2. `pnpm lint` — 2 lintable packages with `--max-warnings 0`
3. `scripts/ci/check-forbidden-patterns.sh` — baseline-aware grep for NEVER-DO rules 11/13/15

**Forbidden-pattern baseline:** currently 66 (ratcheted up from 61 on Apr 17 for 5 new expenses-module `createClient` imports — same grandfathered pattern as 54 pre-existing files). Long-term target: ratchet back down after refactoring these page-level imports into `-queries.ts` helpers.

**Playwright smoke tests** exist (`e2e/smoke.spec.ts`, 9 tests — 6 original + 3 Purchase v2 paths) but not wired into CI yet — needs dev Supabase test user + GitHub Actions secrets.

---

## External registrations in progress

| Item | Where | Blocks | Status |
|------|-------|--------|--------|
| Sungrow iSolarCloud API | isolarcloud.com/developer | Live inverter polling | Registration started, 4–8 weeks |
| Growatt API | server-api.growatt.com | Live inverter polling | Registration started, 4–8 weeks |
| WATI.io WhatsApp BSP | wati.io | Phase 2 direct WA sending | Registration started, 2–4 weeks |
| Facebook Business Manager | business.facebook.com | Required for WATI.io | Part of WATI registration |

---

## Known open issues (non-blocking)

- **Inverter live polling** — adapter stubs + Edge Function + partitioned telemetry all shipped (migration 050 + `packages/inverter-adapters/`). Awaiting Sungrow/Growatt API registration (4–8 weeks). Until then: `SYNTHETIC_INVERTER_READINGS=1` env var produces synthetic readings for end-to-end testing.
- **As-any cleanup R2** — first round cleared 5 files (97→57 baseline violations). ~56 more action/query files remain. Not urgent; discipline gate stops new ones.
- **56 action files** still to migrate to `ActionResult<T>` return shape (NEVER-DO rule #19). Only ~5 done so far.
- **God components** — all 3 files >1,000 LOC from the April 14 audit have been split (survey-form, project-files, proposal-wizard). No known component >500 LOC currently.

---

## What counts as "this week's work"

This file is the weekly view. When something in the table above ships, it:
1. Gets removed from this file.
2. Gets one line added to `docs/CHANGELOG.md`.
3. Updates the relevant module doc in `docs/modules/<module>.md`.

When something new enters the queue, it goes in the table above and (if big enough) gets a spec in `docs/superpowers/specs/`.
