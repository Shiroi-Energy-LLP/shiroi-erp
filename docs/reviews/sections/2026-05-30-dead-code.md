# Dead Code, Duplicates & Refactor Candidates Review — 2026-05-30

> Read-only sweep for unused exports, duplicated logic, oversized files, stale
> TODOs, dead tables, orphan migrations, commented-out blocks, and dependency
> drift. Bias: high confidence over coverage — every flagged dead-symbol was
> verified with a full-repo grep + cross-checked against SQL functions and n8n
> workflows.

## Summary

- **At least 14 application-layer symbols are completely unreferenced.** Two are entire files (`pvwatts.ts`, `ai/project-daily-report.ts` — both ~60+ days dead per `git log`); seven are React components built during the spring feature waves and never wired into a page (`LeadScoreBadge`, `PriceOverrideModal`, `ReferrerChangeDialog`, `RescheduleVisitDialog`, `VisitStatusToggle`, `AwaitingClientToggle`, `TaskWorkLog`); the rest are server actions with no callers (`countPendingClosureApprovals`, `softDeleteDocument`, `listMilestonePhotos`, `listPendingOutreach`, `getTerritory`, `getProjectsWithNoReportToday`, `verifyEntityData`, `cancelRfq`, `disablePartner` + `enablePartner` + `updatePartner` + `updateVendor` + `deleteAmc`, and the `addWorkLog`/`getWorkLogs` pair).
- **~45 application-unused tables remain in the schema** — most are foundation-era (migs 002–006) features never wired up: `plants` (replaced by `plant_monitoring_credentials` mig 059), the entire training/onboarding sub-system (`learning_modules`/`learning_progress`/`training_modules`/`training_questions`/`onboarding_*`/`employee_question_progress`), drip-sequence v1 (`drip_sequences`/`drip_sequence_steps`/`drip_sequence_enrollments` — Phase F replaced with n8n + Meta templates), marketing campaign machinery (`marketing_campaigns`/`marketing_campaign_deliveries`/`message_delivery_log`), warranty/VIP/blacklist tables, employee HR satellites (`employee_documents`/`employee_insurance`/`employee_skills`/`employee_lifecycle_events`/`employee_exit_checklists`/`hr_letters`/`salary_increment_history`/`payroll_export_files`/`payroll_monthly_inputs`/`monthly_attendance_summary`/`attendance_corrections`), proposal sub-engines (`proposal_analytics`/`proposal_scenarios`/`proposal_scope_split`/`proposal_simulations`/`proposal_revisions`/`proposal_otp_log`/`proposal_digital_acceptance`/`proposal_correction_log`), purchase/RFQ duplicates (`rfq_requests` + `rfq_responses` superseded by `rfq_invitations`+`rfq_quotes`; `goods_receipt_notes` + `grn_items` + `three_way_match` + `purchase_order_amendments` + `vendor_delivery_challan_items`), O&M extras (`om_pricing_rules`/`om_profitability`/`om_visit_checklist_items`/`om_visit_corrections`/`photo_gate_verifications`/`plant_daily_summaries`/`plant_data_readings`), and dead bookkeeping (`finance_reports`/`company_cashflow_snapshots`/`customer_checkins`/`customer_quarterly_reports`/`form_interaction_metrics`/`bill_clearing_packages`/`bom_correction_factor_updates`/`channel_partner_leads`/`lead_*` audit satellites/`letters_of_intent`/`record_audit_log`/`referral_rewards`/`regulatory_ecosystem_contacts`/`saved_views`/`stock_replacement_history`/`system_logs`/`system_webhook_failures`/`daily_question_delivery_log`/`language_training_scenarios`/`subcontractor_work_orders`/`price_book_accuracy`/`proforma_invoices`/`project_assignments`/`project_handovers`/`project_issues`/`project_completion_components`/`qc_non_conformance_reports`/`inverter_readings_hourly`/`inverter_string_readings`).
- **19 of the 41 declared `ErpEventName` events are never emitted from ERP code.** The whole `lead.created`/`lead.stage_changed`/`proposal.requested`/`purchase_order.created`/`grn.recorded`/`vendor_payment.due` set is type-allowed but no caller fires them. Workflows #02–#18 wait on events that ERP never sends. (Architecture review #56 noted this for events vs. router cases; this finding is the upstream half — events that ERP never even attempts to emit.) Three events I would have flagged as unused (`lead.win_loss_analysed`, `vendor.bill_ai_approved`, `vendor.bill_ai_rejected`) are actually fired via assigned `const WIN_LOSS_EVENT: ErpEventName = ...` patterns and pass.
- **`formatDate(...)` is reimplemented locally in 13 files** even though the canonical helper lives in `@repo/ui/formatters` and is imported correctly by 80 others. Same content (`new Date(...).toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' })`) duplicated across `(erp)/design/[leadId]/page.tsx`, `(erp)/partners/[id]/page.tsx`, `(erp)/procurement/[poId]/page.tsx`, `components/contacts/activity-timeline.tsx`, `components/data-table/data-table.tsx`, `components/payments/payments-tracker-table.tsx`, `components/projects/cut-length/cut-length-tab.tsx`, `components/projects/lead-files.tsx`, `components/projects/performance/performance-tab.tsx`, `components/projects/project-files/helpers.ts` and 3 more.
- **Time-arithmetic magic numbers (`1000 * 60 * 60 * 24`) are repeated 19+ times** across `ai/task-suggestion-scanner.ts`, `ai/briefing-kpis.ts`, `ai/anomaly-narrator.ts`, `ai/knowledge-qa-rate-limit.ts`, `ai/task-suggestion-cap.ts`, `customer-outreach-actions.ts`, three dashboard pages, two leave-management forms, and others. No `MS_PER_DAY` constant exists.
- **8 server-action files and 4 component files exceed the NEVER-DO #14 500-LOC limit.** `project-step-actions.ts` is the worst (**2522 LOC** — 5× the limit; mixes survey + BOM + BOI + BOQ + commissioning + delivery-challan in one file). `rfq-actions.ts` is 1200 LOC; `po-actions.ts` 830; `procurement-actions.ts` 721. Among forms: `boq-variance-form.tsx` 718, `bom-line-form.tsx` 565, `step-liaison.tsx` 527 (NEVER-DO #14 hard violation), and `data-table/data-table.tsx` 729 (not a form but the most-imported client component). The architecture review captures size as a "split for maintainability" finding; reflagged here because dead-code work is the natural moment to also extract.
- **Two distinct migrations share the `115_*` prefix and migration 124 is missing.** 124 is documented as intentional in `CURRENT_STATUS.md` line 107 (abandoned branch). 115 is not — the 2026-05-24 comprehensive review flagged it and the 2026-05-30 architecture review re-flagged it; still unresolved. `049` and `075` are also absent from the sequence with no documentation.
- **`@hookform/resolvers` is in `apps/erp/package.json` but unused.** No file imports `zodResolver` anywhere in the repo; the package was added at some point but its consumer was removed without removing the dep.
- **`@anthropic-ai/sdk` is imported by 3 ERP files but is NOT declared in `apps/erp/package.json` deps** — it works only because the root `package.json` declares it and pnpm hoists. Inverse of the `@hookform/resolvers` problem and an implicit dep risk.
- **Duplicate n8n workflow numbering**: two files prefixed `29-` exist (`29-drive-folder-create.json` and `29-proposal-sent-to-customer.json`). The `scripts/push-n8n-workflows.ts` script identifies workflows by ID/name, not file prefix, so this is cosmetic — but breaks the "one number, one workflow" convention everywhere else in the directory.
- **`PaginatedResult<T>` interface is defined twice** — once in `contacts-queries.ts` and once in `projects-queries.ts` — same shape, no shared lib. `DocumentCategory` has competing definitions: an interface in `components/projects/project-files/types.ts` and a type alias in `lib/documents-constants.ts`.
- **`tasks-actions.ts` has a complete dead island**: `addWorkLog`, `getWorkLogs`, `WorkLogRow`, `TaskWorkLogInsert`, `TaskWorkLogRow` types + the `TaskWorkLog` UI component. Backing table `task_work_logs` is in the schema. No UI surface calls any of it. The whole feature shipped and was never integrated.

## Findings

| Severity | File:Line / Symbol | Issue | Suggested fix |
|----------|--------------------|-------|---------------|
| HIGH | `apps/erp/src/lib/pvwatts.ts` (entire file, 78 LOC) | `simulatePVOutput` + private helpers — never imported. Last touched 2026-04-01 (~60d). Comment claims PVLib microservice fallback but no caller exists. | Delete the file. NREL PVWatts dependency exists in `anomaly-detector.ts` separately if needed. |
| HIGH | `apps/erp/src/lib/ai/project-daily-report.ts` (entire file) | `generateProjectDailyReport` has zero callers. 2026-05-24 comprehensive review #1 noted it references columns that don't exist (`projects.commissioned_at`, `daily_site_reports.work_completed`). Last touched 2026-05-24. | Delete the file OR rewrite against the real schema + wire to an API route. Right now it's dead AND broken. |
| HIGH | `apps/erp/src/lib/n8n/emit.ts:23-75` ↔ ERP callers | 19 of 41 declared `ErpEventName` events are never emitted by ERP code: `lead.created`, `lead.stage_changed`, `lead.stale_24h`, `lead.quick_quote_sent`, `proposal.requested`, `proposal.submitted`, `proposal.approved`, `proposal.rejected`, `project.installation_scheduled`, `project.installation_complete`, `purchase_order.created`, `grn.recorded`, `vendor_payment.due`, `invoice.overdue`, `om_ticket.resolved`, `leave_request.submitted`, `document.expiring`, `plant.anomaly_detected`, `workflow.error`. Some have downstream n8n workflows waiting (#02 lead-created, #08 vendor-payment-due, #14 customer-payment-received) that will never fire from ERP. | Either (a) wire the missing emits at the appropriate state transitions (best for the n8n workflows already built) or (b) trim the union to only emitted events and stub the downstream workflows. Architecture #56 made the equivalent point from the router side. |
| HIGH | `supabase/migrations/115_liaison_tneb_stages.sql` + `115_referrer_consolidation.sql` | Two migrations share the `115_` prefix. CI's migration ordering becomes filesystem-sort-dependent (currently alphabetical, so `liaison` runs before `referrer`). Was flagged in the 2026-05-24 review and the 2026-05-30 architecture review; still unresolved. | Rename one to `115b_*.sql` per the existing letter-suffix convention (mig 002a/b/c, 003a/b/c, etc.). Pure filesystem rename — no replay risk because both have been applied. |
| HIGH | `supabase/migrations/` — 049, 075, 124 absent | 124 is documented as an intentional gap (abandoned branch). 049 and 075 are also missing with **no documentation** in `CURRENT_STATUS.md` or `CHANGELOG.md`. New contributors looking at `wc -l 049_*.sql` will be confused. | Add a one-line note to `CURRENT_STATUS.md` migration-state row explaining 049 and 075 (or backfill them as no-op placeholders with a comment). |
| HIGH | `apps/erp/src/lib/tasks-actions.ts:217-285` + `components/tasks/task-work-log.tsx` | Complete dead feature: `addWorkLog` server action, `getWorkLogs` query, `WorkLogRow` type, and the `TaskWorkLog` UI component. `task_work_logs` table (defined in migration unknown — referenced from `database.ts`) is in the schema. Zero pages render the component. Last touched 2026-04-08. | If task work-logging is wanted, wire `TaskWorkLog` into the task-detail page or `/tasks/[id]`. If not, delete the action + component + remove the table in a cleanup migration. |
| HIGH | `apps/erp/package.json:16` — `@hookform/resolvers` | Listed as a dep but `zodResolver` (the only function exported) is imported nowhere in the repo. Suggests an aborted form refactor. | Remove from `dependencies`. `pnpm install` will drop the package. |
| HIGH | Root `package.json:30` — `@anthropic-ai/sdk` | Used by 3 ERP files but **not** declared in `apps/erp/package.json`. Works because root deps hoist via pnpm. A future workspace isolation change would break the build silently. | Add `@anthropic-ai/sdk` to `apps/erp/package.json` `dependencies`, where it's actually consumed. |
| HIGH | `apps/erp/src/lib/project-step-actions.ts` (2522 LOC, 45 exports) | 5× the 500-LOC heuristic. Mixes survey CRUD, BOM, BOI, BOQ, commissioning, delivery-challan, milestone seeding, employee dropdown. Architecture review #31 flagged for split; reflag because dead-code cleanup is the natural moment to also extract. | Split into `project-survey-actions.ts`, `project-bom-actions.ts`, `project-boq-actions.ts`, `project-commissioning-actions.ts`, `project-dc-actions.ts`. |
| HIGH | ~45 dead tables across foundation migrations 002–006, plus 050s/060s waves | Application code never reads or writes the tables listed in the summary. Some have triggers that write rows nobody reads (e.g., `lead_status_history`, `proposal_status_history` — purely append-only DB audit logs); those can stay. The rest are pure schema overhead — RLS policies, indexes, and storage costs with zero functional value. | One-shot "schema sweep" migration to `DROP TABLE` the truly unused ones after a 30-day "anything we missed?" window. Suggest categorising into: definitely dead (Phase 1 archive sweep) vs. write-only-audit (keep as-is). |
| MEDIUM | `apps/erp/src/components/leads/lead-score-badge.tsx` | `LeadScoreBadge` is exported and imported by no other file. Built for "C1 AI lead scores" but never wired into a leads list. Last touched 2026-05-24 (very recent). | Wire into the leads table column or `LeadCard`, or delete. |
| MEDIUM | `apps/erp/src/components/proposals/price-override-modal.tsx` | `PriceOverrideModal` has zero callers. Last touched 2026-04-05 (~55d). | Delete or wire into the proposal pricing screen. |
| MEDIUM | `apps/erp/src/components/leads/referrer-change-dialog.tsx` | `ReferrerChangeDialog` has zero callers. Last touched 2026-05-23. The `lead-referrer-actions.ts` server action it would call appears wired elsewhere. | Wire into the lead detail page (`/leads/[id]`) referrer row or delete. |
| MEDIUM | `apps/erp/src/components/om/reschedule-visit-dialog.tsx` + `visit-status-toggle.tsx` | Both exported with zero callers. Last touched 2026-04-12 (~50d). | Delete; the OM visit-list page (`(erp)/om/visits/page.tsx`) does not use them. |
| MEDIUM | `apps/erp/src/components/liaison/awaiting-client-toggle.tsx` | `AwaitingClientToggle` has zero callers. Last touched 2026-05-23. The `setAwaitingClientDetails` action it'd invoke is implemented in `liaison-actions.ts`. | Wire into `/liaison/[projectId]` detail page or delete. |
| MEDIUM | 14 unused server-action exports across `lib/` | `countPendingClosureApprovals` (closure-queries.ts), `softDeleteDocument` (documents-actions.ts), `listMilestonePhotos` (milestone-photos-actions.ts), `listPendingOutreach` (customer-outreach-actions.ts), `getTerritory` (sales-territories-queries.ts), `getProjectsWithNoReportToday` (dashboard-queries.ts), `verifyEntityData` (data-flag-actions.ts), `cancelRfq` (rfq-actions.ts), `disablePartner`/`enablePartner`/`updatePartner` (partners-actions.ts), `updateVendor` (vendor-actions.ts), `deleteAmc` (amc-actions.ts), `createProposalAction` (proposal-actions.ts), `archiveLead`/`unarchiveLead` (leads-actions.ts), `batchApproveQueueItems`/`batchRejectQueueItems` (whatsapp-import-actions.ts), `createObjection` (liaison-actions.ts), `addCostVariance` (project-step-actions.ts), `allocateToProject` (inventory-actions.ts), `endContactCompanyRole` (contacts-actions.ts). | One sweep deletion + grep-confirm pass. Some are clearly half-built features (e.g. `archiveLead`/`unarchiveLead` pair) — confirm with Vivek whether to wire or kill. |
| MEDIUM | `formatDate` redefined in 13 files | Canonical `formatDate` lives in `@repo/ui/formatters` and is imported by 80 other files. 13 files reimplement it locally. Same content body. | Replace each local definition with `import { formatDate } from '@repo/ui/formatters'`. Verify in particular `components/data-table/data-table.tsx:23` because it's loaded by every list page. |
| MEDIUM | 19+ inline `1000 * 60 * 60 * 24` magic numbers | Same time-millisecond conversion across `ai/task-suggestion-scanner.ts` (3×), `ai/briefing-kpis.ts` (4×), `ai/anomaly-narrator.ts`, `ai/knowledge-qa-rate-limit.ts`, `ai/task-suggestion-cap.ts`, `customer-outreach-actions.ts`, `(erp)/design/page.tsx`, `(erp)/hr/certifications/page.tsx` (2× — also a duplicate within the same file), `(erp)/om/profitability/page.tsx`, `(erp)/dashboard/purchase-dashboard.tsx`, `(erp)/ask/page.tsx`, `components/hr/leave-request-form.tsx`. | Add `MS_PER_DAY = 1000 * 60 * 60 * 24` and `MS_PER_HOUR` to `@repo/ui/time-helpers` (or `apps/erp/src/lib/time-helpers.ts`), import everywhere. |
| MEDIUM | `apps/erp/src/app/(erp)/hr/certifications/page.tsx:18-40` | `expiryStatus` and `expiryClassName` are two helpers that both compute `diffDays` from the same `expiryDate + 'T00:00:00+05:30'` via duplicate `Math.ceil(diffMs / (1000 * 60 * 60 * 24))` blocks. | Extract single `getExpiryInfo(expiryDate): { diffDays, label, variant, className }` helper. |
| MEDIUM | `daysWaiting` in `(erp)/design/page.tsx:30` vs `daysSinceDelivery` in `lib/msme-calc.ts:15` | Two helpers solving "days between" with slightly different signatures. `daysWaiting` is local to one page; `daysSinceDelivery` is exported but takes `Date` instead of `string`. | Unify on a single `daysBetween(from: string \| Date, to?: string \| Date)` helper in `@repo/ui` or `lib/time-helpers.ts`. |
| MEDIUM | `apps/erp/src/lib/contacts-queries.ts:3` and `projects-queries.ts:15` | `PaginatedResult<T>` interface defined identically in both files. Same shape: `{ data: T[]; total: number; hasMore: boolean }`. | Move to `lib/types/pagination.ts`, import from both. |
| MEDIUM | `apps/erp/src/components/projects/project-files/types.ts:80` + `lib/documents-constants.ts:13` | `DocumentCategory` is an `interface` in one file and a `type` alias in the other. Different shapes; same name; both exported. Risk of import-path-dependent type drift. | Rename one (the file-tab one is the older, simpler one — call it `FileCategory`) or unify if they really should be the same. |
| MEDIUM | 103 inline `const { data: { user } } = await supabase.auth.getUser()` blocks | Duplicated auth-pattern across `apps/erp/src/lib/*.ts` server actions, with copy-paste null-check + error branch each time. | Extract `requireAuthUser()` helper in `lib/auth.ts` that returns `{ user } \| { error: ActionResult }`. (`requireAuth` exists but `redirect()`s — only useful for Server Components, not for action files which need typed error returns.) |
| MEDIUM | `apps/erp/src/lib/finance-actions.ts:601-611` and `:642-646` | Two commented-out block of 10+ lines each (GSP HTTP call placeholder + post-GSP IRN storage). Real code in between is the stub. Stale-comment risk when GSP integration finally lands. | Either move the placeholder to `docs/` as an integration guide, or write it as the real implementation behind a feature flag. Don't leave 20 lines of commented-out HTTP code in the actions file. |
| MEDIUM | `packages/inverter-adapters/src/index.ts:14-22` | Header comment lists `sungrow` and `growatt` as `STUB (synthetic mode works, live HTTP TODO)` — but per `CHANGELOG.md` and Phase 8 (2026-05-24), both adapters were rewritten with live HTTP implementations. Comment is stale. | Update the status block to reflect Phase 8: sungrow=LIVE (requires Authorize click for OAuth), growatt=LIVE with session-cache caveat; others remain stubs. |
| MEDIUM | `packages/inverter-adapters/src/growatt.ts:34` | `TODO(phase-8-session-cache)` for cookie caching. Per CHANGELOG line 49, Phase 8 ADDED a Growatt session cache. The TODO should either describe what's still missing or be deleted. | Verify what the cache layer covers; delete the TODO if Phase 8 closed it. |
| MEDIUM | `infrastructure/n8n/workflows/29-drive-folder-create.json` + `29-proposal-sent-to-customer.json` | Two workflow files share the `29-` prefix. The push script (`scripts/push-n8n-workflows.ts`) keys by ID/name so this doesn't break deploys, but every other number in the directory is unique. | Renumber one (`29b-` or a new sequential number) to keep the directory readable. |
| MEDIUM | `apps/erp/src/components/data-table/data-table.tsx` (729 LOC) — also flagged by Architecture review | Single client component handling sorting + filtering + pagination + column-picker + bulk select + view tabs. Most-loaded client component in the app. | Split per Architecture #46. Also: its local `formatDate` at line 23 should import from `@repo/ui/formatters`. |
| MEDIUM | `apps/erp/src/lib/customer-outreach-actions.ts:34` (generateCustomerCheckinsForWeek) | Flagged by 2026-05-24 comprehensive review #7 as security risk (admin client, no role check). Architecture review #15 said "expressed via metadata check, brittle." Reflag for a different angle: function exists, n8n workflow #30 ostensibly calls it via the cron, but `customer_checkins` table is in the `LOW-REF=0` dead-table list and `listPendingOutreach` (which would read it) is also unused. The whole feature loop may be dead. | Verify n8n workflow #30 is active and writing — if so, why is `listPendingOutreach` unused? If #30 is inactive, kill the whole sub-system (action + n8n workflow + customer_checkins table) until ready. |
| LOW | `apps/erp/src/lib/pdf/proposal/quote-constants.ts:5,18` | Two TODOs from 2026-05-20: "move BANK values to a settings table when /settings/company UI exists." No `/settings/company` page exists today. ~10 days old; reasonable to leave but should be tracked. | Either build the `/settings/company` tab (small) or move the TODO into `docs/CURRENT_STATUS.md` as a tracked decision. |
| LOW | `apps/erp/src/lib/handover-actions.ts:100` | `phone: '+91-XXXXXXXXXX'` hardcoded placeholder. Looks like a debug stub left in. | Pull from `system_settings` or fail loud if missing — never default to an XXXXXX phone string. |
| LOW | `apps/erp/src/lib/ai/anthropic-client.ts:26` (`MAX_TOKENS_SHORT`) | Exported constant `MAX_TOKENS_SHORT = 256` with zero callers anywhere in the repo. (`MAX_TOKENS_GENERATION` and `generateText` are used; `MAX_TOKENS_SHORT` is not.) | Remove the export. |
| LOW | `apps/erp/src/lib/ai/anthropic-client.ts:32-47` (`getAnthropicClient`) | The `Anthropic` SDK singleton factory exported but no caller imports it. All callers use the higher-level `generateText` helper. The `Anthropic` import remains in the file via `generateText`'s body. | Keep `_client` and inline-construct inside `generateText`. Or keep the export if it's part of the public API; document so future callers know to prefer `generateText`. |
| LOW | `apps/erp/src/lib/ai/anthropic-client.ts:20` (`CLAUDE_MODEL`) | Constant exported but only consumed inside the same file. | Demote to a non-exported `const`. (Architecture review #44 noted the broader pattern around AI module trust boundaries; this is the small-fish version.) |
| LOW | Inverter `index.ts` "current implementation status" comment block | Same staleness as above — describes pre-Phase-8 state. Phase 8 (2026-05-24) made both adapters LIVE. | Rewrite the status block to reflect today's reality. |

## Safe to fix overnight (mechanical, no judgment needed)

- Delete `apps/erp/src/lib/pvwatts.ts` (78 LOC) — zero callers, 60+ days old.
- Delete or rewrite `apps/erp/src/lib/ai/project-daily-report.ts` — zero callers and references columns that don't exist (per 2026-05-24 comprehensive review #1).
- Rename `supabase/migrations/115_referrer_consolidation.sql` → `115b_referrer_consolidation.sql`. Pure filesystem rename; no schema impact (already applied to both dev and prod).
- Rename one of the two `29-*.json` n8n workflow files; e.g., `29-drive-folder-create.json` → `31-drive-folder-create.json` (no other `31-` exists).
- Remove `@hookform/resolvers` from `apps/erp/package.json` dependencies — no caller imports `zodResolver` anywhere.
- Add `@anthropic-ai/sdk` to `apps/erp/package.json` dependencies (move from being only in root). Three files in `apps/erp/src/lib/ai/` import it.
- Extract `MS_PER_DAY = 1000 * 60 * 60 * 24` constant to `apps/erp/src/lib/time-helpers.ts` and replace the 19+ inline occurrences.
- Move `PaginatedResult<T>` to `apps/erp/src/lib/types/pagination.ts`, import from `contacts-queries.ts` and `projects-queries.ts`.
- Replace 13 local `formatDate` implementations with `import { formatDate } from '@repo/ui/formatters'`. Use TypeScript build-type check to verify signature compatibility per file.
- Remove `MAX_TOKENS_SHORT` export from `apps/erp/src/lib/ai/anthropic-client.ts` — no caller.
- Update the stale "STUB (live HTTP TODO)" comment in `packages/inverter-adapters/src/index.ts` to reflect Phase 8 LIVE state.

## Needs Vivek review (judgment calls)

- **Whether to delete vs. wire the seven unused UI components.** `LeadScoreBadge`, `PriceOverrideModal`, `ReferrerChangeDialog`, `RescheduleVisitDialog`, `VisitStatusToggle`, `AwaitingClientToggle`, `TaskWorkLog`. Each was built deliberately for a use case (3 in late May, 4 in early April); some of those use cases may still be wanted but were dropped during the multi-agent wave. Audit + decide per-component.
- **Whether `task_work_logs` is a wanted feature.** Server action + UI component + table exist but nothing wires them. Either ship the feature (~half day of work to add to task detail) or drop the table + actions + component.
- **The 19 declared-but-never-fired ErpEventName entries.** Some have backing n8n workflows (#02 lead-created has a real workflow; #14 customer-payment-received has a workflow). Decide: wire the emits at the right state transitions (best aligns with built workflows) or remove the events from the union AND the workflows together. Half-built leak.
- **The 45-table schema sweep.** Most are clearly dead (`plants` was replaced by `plant_monitoring_credentials`; `rfq_requests`+`rfq_responses` were superseded by `rfq_invitations`+`rfq_quotes`). Some may be aspirational and the founder wants to keep (`vip_contacts`, `warranty_claims`). Need a per-table decision; suggest categorising:
  - **Sweep now:** v1 superseded tables, training/onboarding sub-system, marketing campaign v1, drip-sequence v1.
  - **Keep but document as deferred:** Warranty/VIP/MSME satellites with planned use.
  - **Audit-only tables:** `lead_status_history`, `proposal_status_history`, `record_audit_log` — these get trigger writes that ARE useful even without UI reads.
- **The 17 legacy `{success, error?}` action shapes** (per Architecture #5) — dead-code review intersects here because some unused exports also use the legacy shape; converting + deleting in one pass is more efficient than two separate sweeps.
- **`project-step-actions.ts` 2522-LOC split.** Architecture flagged for split (#31); dead-code review re-flags because the natural moment to split is also the moment to delete the unused exports (`addCostVariance`, `updateCostVariance`). Combined sweep would extract sub-files AND delete the dead exports inside them in one PR.
- **103 duplicated auth blocks.** Architecture review's #44 noted the trust-pattern issue separately; the dead-code lens is "this is just plain duplication." Net result is the same — extract a helper — but the framing affects priority.
