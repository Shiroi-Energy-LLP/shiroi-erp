---
category: Forms
keywords: select, dropdown, native select
---
Native `<select>` with the same chrome as `Input` (1.5px border, gold focus). Pass `<option>` children. Use for short, fixed option lists (project stage, TNEB circle). For rich/searchable menus, use `DropdownMenu` instead.
