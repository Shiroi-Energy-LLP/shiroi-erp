---
category: Navigation
keywords: pagination, pager, pages
---
List pager. Pass `currentPage`, `totalPages`, `pageSize`, `totalItems`, `basePath`, and `entityName`; it renders "Showing X–Y of Z entities" with Indian number formatting plus page links (returns null when there's only one page). The active page is a solar-gold chip. Preserve filters via `filterParams`.
