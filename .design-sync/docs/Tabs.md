---
category: Navigation
keywords: tabs, sections, segmented
---
Segmented in-page navigation. Compose `Tabs` (with `defaultValue`) › `TabsList` › `TabsTrigger` (one per section) and matching `TabsContent` panels. The active trigger is a white pill on the `n-100` track. Use for switching views within one record (Overview / BOM / Timeline / Documents).
