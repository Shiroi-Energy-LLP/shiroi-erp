---
category: Forms
keywords: checkbox, toggle, indeterminate
---
Checkbox with a solar-gold checked fill and dark-ink tick. Supports `checked`, `defaultChecked`, `disabled`, and `indeterminate` (renders a minus — use for partial selections like "2 of 5 uploaded"). Pair with a `Label` bound via `htmlFor`/`id`.
