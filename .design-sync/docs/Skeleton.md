---
category: Data Display
keywords: skeleton, loading, placeholder, shimmer
---
Pulsing placeholder for loading states. Size it with `className` (`h-4 w-40`, `rounded-full` for avatars) and compose several to mirror the real layout you're loading — a card, a list row, a table. Warm `n-200` fill.
