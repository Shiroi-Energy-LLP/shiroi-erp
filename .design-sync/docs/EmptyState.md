---
category: Data Display
keywords: empty state, zero state, placeholder
---
Centered empty state for zero-data views. Pass `title`, optional `description`, an optional `icon` (a Lucide glyph), and an `action` (usually a `Button`). Follow the ERP voice: name the entity and the next action — "No proposals yet → Create your first proposal", never "No data found".
