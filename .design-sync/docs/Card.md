---
category: Data Display
keywords: card, panel, surface
---
White surface with a warm hairline border, 12px radius, and xs shadow. Compose `Card` › `CardHeader` (`CardTitle` + `CardDescription`) › `CardContent` › `CardFooter`. `CardTitle` is Archivo bold. Keep the content area light — no coloured fills. Add hover elevation via `className` for clickable cards.
