---
category: Forms
keywords: input, text field, form
---
Single-line text input with a 1.5px border and gold focus ring. Native `<input>` — pass any input attributes (`type`, `placeholder`, `disabled`, `value`). Pair with `Label` above and a hint below. For an error state, add a red border via `className`. Height 36px to match `Select` and `Button` default.
