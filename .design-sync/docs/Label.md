---
category: Forms
keywords: label, field label, form
---
Field label — 12px semibold, sits above an `Input`/`Select` or beside a `Checkbox`. Use `htmlFor` to bind it to the control's `id`. Add a red asterisk span for required fields.
