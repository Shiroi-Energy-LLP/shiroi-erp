# Copy to .env on the droplet (never commit the real .env).
# Generate a fresh key with: openssl rand -hex 32
# Save the value in the password manager — losing it makes all stored n8n credentials unreadable.
N8N_ENCRYPTION_KEY=REPLACE_WITH_64_HEX_CHARS
